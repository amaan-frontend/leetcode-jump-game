/**
 * @param {number[]} nums
 * @return {number}
 */
var jump = function (nums) {
  const n = nums.length;
  if (n === 1) return 0;

  let maxReach = nums[0];
  let steps = 1;
  return steps;
};
